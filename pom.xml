<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>cc.wanforme.nukkit</groupId>
	<artifactId>StarterDemo</artifactId>
	<version>1.0-SNAPSHOT</version>
	<packaging>jar</packaging>

	<name>StarterDemo</name>
	<url>http://maven.apache.org</url>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<spring-boot-starter-test.version>2.3.1.RELEASE</spring-boot-starter-test.version>
		<spring-boot-devtools.version>2.3.1.RELEASE</spring-boot-devtools.version>
		<nukkit.version>2.0.0-SNAPSHOT</nukkit.version>
	</properties>

	<dependencies>
		<dependency>
			<groupId>cc.wanforme.nukkit</groupId>
			<artifactId>wannukkit-springboot-starter</artifactId>
			<version>1.0-SNAPSHOT</version>
		</dependency>

		<!-- <dependency> <groupId>cn.nukkit</groupId> <artifactId>nukkit</artifactId> 
			<version>${nukkit.version}</version> <scope>provided</scope> <optional>true</optional> 
			</dependency> -->
	</dependencies>

	<build>
		<!-- 默认源代码和资源文件目录配置 -->
		<resources>
			<resource>
				<directory>${project.basedir}/src/main/java</directory>
			</resource>
			<resource>
				<directory>${project.basedir}/src/main/resources</directory>
			</resource>
		</resources>
		<testResources>
			<testResource>
				<directory>${project.basedir}/src/test/java</directory>
			</testResource>
			<testResource>
				<directory>${project.basedir}/src/test/resources</directory>
			</testResource>
		</testResources>

		<!-- <outputDirectory>${project.basedir}/nukkit</outputDirectory> <testOutputDirectory>${project.basedir}/nukkitTest</testOutputDirectory> -->

		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<!-- 跳过 BOOT-INF 文件夹 -->
					<!--<skip>true</skip> -->
					<!-- live load. 热部署[2] -->
					<!-- <fork>true</fork> -->
					<!-- Main Class. 指定主方法 -->
					<mainClass>cc.wanforme.nukkit.springDemo.StarterDemo</mainClass>
					<!-- 剔除 nukkit -->
					<!-- 
					<excludes>
						<exclude>
							<groupId>cn.nukkit</groupId>
							<artifactId>nukkit</artifactId>
						</exclude>
					</excludes>
					 -->
					<executable>true</executable>
				</configuration>
				<executions>
					<execution>
						<goals>
							<goal>repackage</goal>
						</goals>
					</execution>
				</executions>
				<dependencies>
					<dependency>
						<groupId>org.springframework.boot.experimental</groupId>
						<artifactId>spring-boot-thin-layout</artifactId>
						<version>1.0.25.RELEASE</version>
					</dependency>
				</dependencies>
			</plugin>
			<plugin>
                <groupId>org.springframework.boot.experimental</groupId>
                <artifactId>spring-boot-thin-maven-plugin</artifactId>
                <version>1.0.25.RELEASE</version>
                <executions>
                    <execution>
                        <!--在编译时下载依赖包 -->
                        <id>resolve</id>
                        <goals>
                            <goal>resolve</goal>
                        </goals>
                        <inherited>false</inherited>
                    </execution>
                </executions>
            </plugin>
		</plugins>
	</build>
</project>
